import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

type Props = {}

const Home = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>Home</Text>
    </SafeAreaView>
  )
}

export default Home